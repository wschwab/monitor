[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
via_ir = true

[profile.default.fuzz]
runs = 256

[profile.ci]
fuzz = { runs = 1000 }
verbosity = 4

[rpc_endpoints]
tempo = "${TEMPO_RPC_URL}"